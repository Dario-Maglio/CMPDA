install git pip pylint

python3 -m pip install --user numpy scipy matplotlib ipython jupyter pandas sympy pytest

# use python3 -m to run module such as notebook, ipython, sympy
# or add export PATH=$PATH:~/.local/bin to .bashrc

To execute fortran subrutines in python
python -m numpy.f2py -c -m aaa aaa.f
import aaa
